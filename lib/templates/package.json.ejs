{
  "name": "<%- pkgName %>",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "predeploy": "aws cloudformation package --template-file template.yaml --output-template-file output-template.yaml --s3-bucket <%- bucketName %>",
    "deploy": "aws cloudformation deploy --stack-name <%- stackName %> --template-file output-template.yaml --capabilities CAPABILITY_NAMED_IAM"
  }
}
